<Window x:Class="DesktopMusicPlayer.MiniPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopMusicPlayer"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Mini Player" 
        MinWidth="380" MaxWidth="700"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        Opacity="0"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ResizeMode="NoResize"
        ShowInTaskbar="False">



    <!-- Theme-aware Border -->
    <Border x:Name="MainBorder" Background="{DynamicResource MiniPlayerBackgroundBrush}" CornerRadius="12" 
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
            MouseLeftButtonDown="Window_MouseLeftButtonDown"
            Padding="16,12">
        <Border.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="InfoPanel" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                        <DoubleAnimation Storyboard.TargetName="ControlsOverlay" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                 <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="InfoPanel" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.3"/>
                        <DoubleAnimation Storyboard.TargetName="ControlsOverlay" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/> <!-- Image -->
                <ColumnDefinition Width="*"/>    <!-- Text -->
                <ColumnDefinition Width="Auto"/> <!-- Controls -->
            </Grid.ColumnDefinitions>

            <!-- Cover Art (Spinning Disc) -->
            <Border Grid.Column="0" Width="48" Height="48" CornerRadius="24" Margin="0,0,12,0" Background="Transparent">
                <Grid>
                     <!-- Fallback Icon (Spinning when playing) -->
                    <Grid RenderTransformOrigin="0.5,0.5"
                          Visibility="{Binding CurrentSong.HasCoverArt, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                        <Grid.RenderTransform>
                            <RotateTransform Angle="0"/>
                        </Grid.RenderTransform>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard x:Name="SpinFallbackStoryboard">
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(RotateTransform.Angle)"
                                                                     By="360"
                                                                     Duration="0:0:4"
                                                                     RepeatBehavior="Forever"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <PauseStoryboard BeginStoryboardName="SpinFallbackStoryboard"/>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Image Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"
                               RenderTransformOrigin="0.5,0.5">
                            <Image.Source>
                                <BitmapImage UriSource="/Assets/Spin-disk2.png" DecodePixelWidth="128"/>
                            </Image.Source>
                            <Image.RenderTransform>
                                <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                            </Image.RenderTransform>
                            <Image.Clip>
                                <EllipseGeometry Center="24,24" RadiusX="24" RadiusY="24"/>
                            </Image.Clip>
                        </Image>
                    </Grid>

                    <!-- Actual Spinning Image -->
                    <Grid RenderTransformOrigin="0.5,0.5"
                          Visibility="{Binding CurrentSong.HasCoverArt, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid.RenderTransform>
                            <RotateTransform Angle="0"/>
                        </Grid.RenderTransform>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard x:Name="SpinStoryboard">
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(RotateTransform.Angle)"
                                                                     By="360"
                                                                     Duration="0:0:4"
                                                                     RepeatBehavior="Forever"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <PauseStoryboard BeginStoryboardName="SpinStoryboard"/>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Image Source="{Binding CurrentSong.CoverArt}" Stretch="UniformToFill">
                            <Image.Clip>
                                <EllipseGeometry Center="24,24" RadiusX="24" RadiusY="24"/>
                            </Image.Clip>
                        </Image>
                    </Grid>

                    <!-- Vinyl Center (Layered: Dark Ring + Silver Dot) -->
                    <Ellipse Width="14" Height="14" Fill="#1A1A1A" HorizontalAlignment="Center" VerticalAlignment="Center"
                             Visibility="{Binding CurrentSong.HasCoverArt, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}"/>
                    <Ellipse Width="5" Height="5" Fill="#C0C0C0" HorizontalAlignment="Center" VerticalAlignment="Center"
                             Visibility="{Binding CurrentSong.HasCoverArt, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}"/>
                </Grid>
            </Border>

            <!-- Central Area: Layered Info & Controls -->
            <Grid Grid.Column="1" Margin="0,0,12,0" VerticalAlignment="Center">
                
                <!-- Layer A: Metadata (Default Visible) -->
                <StackPanel x:Name="InfoPanel" VerticalAlignment="Center" MaxWidth="220">
                    <TextBlock Text="{Binding CurrentSong.Title, FallbackValue='No Song Playing'}" 
                               Foreground="{DynamicResource PrimaryTextBrush}" FontWeight="SemiBold" FontSize="14" 
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding CurrentSong.Artist, FallbackValue='Crescendo'}" 
                               Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" 
                               TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Layer B: Controls Overlay (Default Hidden) -->
                <Grid x:Name="ControlsOverlay" Opacity="0" VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Buttons -->
                        <RowDefinition Height="Auto"/> <!-- Slider -->
                    </Grid.RowDefinitions>

                    <!-- Playback Buttons -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                        <!-- Shuffle -->
                        <Button Command="{Binding ShuffleCommand}" Margin="0,0,8,0" ToolTip="Shuffle" Width="28" Height="28">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
                                    <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsShuffleEnabled}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                            <Setter Property="ToolTip" Value="Shuffle On"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="&#xE8B1;" FontFamily="Segoe Fluent Icons" FontSize="12"/>
                        </Button>

                        <Button Style="{StaticResource IconButtonStyle}" Command="{Binding PreviousCommand}" Margin="0,0,8,0" ToolTip="Previous" Width="32" Height="32">
                            <TextBlock Text="&#xE892;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </Button>

                        <Button Width="36" Height="36" Command="{Binding PlayPauseCommand}" Margin="0,0,8,0" ToolTip="Play/Pause">
                             <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bdr" Background="#87CEEB" CornerRadius="18">
                                        <Grid>
                                            <TextBlock Text="&#xE768;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="White"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       Visibility="{Binding IsPlaying, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                            <TextBlock Text="&#xE769;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="White"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       Visibility="{Binding IsPlaying, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bdr" Property="Background" Value="#9FD9F6"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Style="{StaticResource IconButtonStyle}" Command="{Binding NextCommand}" Margin="0,0,8,0" ToolTip="Next" Width="32" Height="32">
                            <TextBlock Text="&#xE893;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </Button>

                        <!-- Repeat -->
                        <Button Command="{Binding RepeatCommand}" ToolTip="Repeat" Width="28" Height="28">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
                                    <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RepeatMode}" Value="RepeatAll">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                            <Setter Property="ToolTip" Value="Repeat All"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding RepeatMode}" Value="RepeatOne">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                            <Setter Property="ToolTip" Value="Repeat One"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="12">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="&#xE8EE;"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding RepeatMode}" Value="RepeatOne">
                                                <Setter Property="Text" Value="&#xE8ED;"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Button>
                    </StackPanel>

                    <!-- Compact Slider -->
                    <Grid Grid.Row="1" Height="14" VerticalAlignment="Center">
                         <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="{Binding CurrentTimeFormatted}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        
                        <Slider Grid.Column="1" Value="{Binding CurrentProgress}" Maximum="{Binding TotalDuration}" 
                                IsMoveToPointEnabled="True" Cursor="Hand" VerticalAlignment="Center" Focusable="False"
                                Thumb.DragStarted="Slider_DragStarted" Thumb.DragCompleted="Slider_DragCompleted"
                                PreviewMouseLeftButtonDown="Slider_PreviewMouseLeftButtonDown" PreviewMouseLeftButtonUp="Slider_PreviewMouseLeftButtonUp">
                             <Slider.Style>
                                <Style TargetType="Slider" BasedOn="{StaticResource ProgressSliderStyle}">
                                     <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentSong}" Value="{x:Null}">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Slider.Style>
                        </Slider>
                        
                         <TextBlock Grid.Column="2" Text="{Binding TotalTimeFormatted}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Compact Controls (Only Expand) -->
            <StackPanel x:Name="ExpandControlsPanel" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                 <!-- Expand -->
                <Button Style="{StaticResource IconButtonStyle}" Click="ExpandButton_Click" ToolTip="Expand" Width="32" Height="32">
                    <TextBlock Text="&#xE740;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}"/>
                </Button>
            </StackPanel>
        </Grid>

    </Border>
</Window>

